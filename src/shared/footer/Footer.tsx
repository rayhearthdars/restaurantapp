import React from "react";
import "./Footer.css";

// No need to wrap it inside the router as there is no link
export const Footer = () => {
    return (
        <footer>
            <p>Gabrielle B.</p>
        </footer>
    )
};
